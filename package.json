{
  "name": "fitness-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Obsidian plugin: gym + golf heatmaps, dashboard, cue rollup, and session creation",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "node --experimental-strip-types --test tests/core.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.28.1",
    "obsidian": "^1.5.7-1",
    "typescript": "^5.3.3"
  }
}
